SCP Are inherited 

Attached to an org -> will drop down to all under thing bellow it 
OU -> All accounts inside
Account -> everything inside 

Management account : Only AWS account which can be restricted using SCP

- SCPs are account permissions boundaries 
- Limit what an account can do (including root user) :
	- We never can restrict the account root user 
	- Restricting the account has an impact on the underlaying root user